{
  "name": "dsh-studio",
  "version": "0.1.0",
  "description": "DSH Studio — DeepSeek Harness 桌面版外壳",
  "main": "main/main.js",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "bundle:dsh": "node scripts/prepare-bundle.js",
    "start": "electron .",
    "dist": "electron-builder --win",
    "dist:full": "npm run bundle:dsh && electron-builder --win"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  }
}
